version: '3'
services:
  database:
    image: "postgres" # use latest official postgres version
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=dybi
    volumes:
      - ./pg-data/:/home/pg-data/ # persist data even if container shuts down
    ports:
      - 5432:5432
    dns_search: 'postgres-db'
